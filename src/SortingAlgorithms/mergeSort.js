export const getMergeSort = (array) => {
    const animations = [];
    if (array.length <= 1) return array;

  };

  function mergeSortHelper(
    mainArray,
    startIdx,
    endIdx,
    auxiliaryArray,
    animations,
  ){

  }

  function merge(
    mainArray,
    startIdx,
    middleIdx,
    endIdx,
    auxiliaryArray,
    animations,
  ){

  }